# --- v1 ML sandbox ---
# 🟢 off-the-shelf: pose + video decode + numerics
mediapipe>=0.10.9        # pose estimation (33 landmarks), CPU-friendly
opencv-python>=4.9.0     # video decode / frame extraction
numpy>=1.26.0            # all feature math lives on ndarrays

# testing / data inspection
pytest>=8.0.0
matplotlib>=3.8.0        # sandbox plots (visibility histograms etc.), not shipped

# --- API + worker (services/) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9  # multipart upload parsing for FastAPI
rq>=2.0.0                # job queue on Redis
redis>=5.0.0

# NOTE: supabase (DB/auth/storage) is added when we wire real persistence + OAuth.
# Pin RTMPose/rtmlib later if/when we upgrade the PoseEstimator.
